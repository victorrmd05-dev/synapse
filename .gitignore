# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# AI Agents and Tools
.agents/
.gemini/
.mcp.json
skills-lock.json

# Paperclip Agents & Skills (Local Only)
paperclip_config/

# Design references — repo-ferramenta (ui-ux-pro-max) é só dev-time (uso local +
# MCP Magic). Pesado (screenshots/cli/python) e o agente runtime não o consome.
# Já a biblioteca de marcas (awesome-design-md/*.md) É versionada — o motor de
# injeção dinâmica em /api/design/generate lê os DESIGN.md dela em runtime.
agentes/designer-webmaster/references/ui-ux-pro-max-skill/
agentes/designer-webmaster/references/awesome-design-md/.github/
# dependencies
node_modules
.pnp
.pnp.js
.yarn/install-state.gz

# testing
coverage

# next.js
.next/
out/

# production
build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# planning
.planning/codebase

# temp and scratch files
scratch/
analyze_*.py

# landing pages geradas pelo Designer (artefatos locais p/ edição via terminal)
lps/

# environment variables
.env

# prints do Gerenciador de Eventos (Meta) — só inspeção local (podem ter dados da conta)
prints_gerenciador/*
!prints_gerenciador/LEIA-ME.md
